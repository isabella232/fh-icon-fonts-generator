# Findhotel Icon Fonts Generator

These repo is converts the custom SVGs files to icon fonts and css.

To run this repo, add your SVGs to under the svg file than run.

```
npm install & npm start
```
